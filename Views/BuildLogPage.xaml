<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Woo_.Views.BuildLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Woo_.Converters"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:LogSeverityBrushConverter x:Key="LogSeverityBrush" />
    </Page.Resources>

    <Grid Padding="28" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Content="Back" Click="Back_Click" />
            <Button
                Grid.Column="1"
                Content="Stop"
                Command="{Binding StopBuildCommand}" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <TextBlock Text="Build Log" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="{Binding BuildStatusText}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock Grid.Column="3" Text="{Binding ElapsedTimeText}" VerticalAlignment="Center" />
            <Button
                Grid.Column="4"
                Content="Open Folder"
                Click="OpenOutputFolder_Click"
                Visibility="{Binding CanOpenOutputFolder, Converter={StaticResource BoolToVisibility}}" />
            <Button Grid.Column="5" Content="Clear" Command="{Binding ClearLogCommand}" />
            <Button Grid.Column="6" Content="Copy" Command="{Binding CopyLogCommand}" />
            <Button Grid.Column="7" Content="Save Log..." Click="SaveLog_Click" />
        </Grid>

        <ProgressBar
            Grid.Row="1"
            Minimum="0"
            Maximum="100"
            Value="{Binding BuildProgress}" />

        <Border
            Grid.Row="2"
            Padding="12"
            CornerRadius="8"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            Background="#15171B">
            <ScrollViewer
                x:Name="LogScrollViewer"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <RichTextBlock
                        x:Name="LogRichTextBlock"
                        IsTextSelectionEnabled="True"
                        FontFamily="{StaticResource MonospaceFont}"
                        TextWrapping="WrapWholeWords" />
                    <Border x:Name="LogBottomAnchor" Height="1" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
